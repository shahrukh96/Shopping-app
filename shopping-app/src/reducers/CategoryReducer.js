import * as c from '../constants/CategoryConstant';

var insitialState={
    categories:[
        {id:1345,name:'Electronics',description:"Item Related to Electronics"},
        {id:5468,name:'Grocery',description:"Item Related Daily Home Needs"}
    ]
};

export default function categoryReducer(state=insitialState,action){

    return state;
}